import UpdateItem from "./components/UpdateItem";

const App = () => {
  const itemId = "1"; // Example: Fetch and update the door with ID 1

  return <UpdateItem itemId={itemId} />;
};

export default App;